# Booking Creation — Hollywood Monsoon Night (Single & Multi-Ticket)

**Feature area:** Booking Creation — book tickets for the static "Hollywood Monsoon Night — Los Angeles" event, capture booking ID and cost, and verify all detail fields on the booking detail page  
**Generated:** 2026-06-02  
**Total scenarios:** 2

| TC No | Category | Preconditions | Steps | Expected Result | Business Rules | Suggested Layer |
|---|---|---|---|---|---|---|
| HMN-001 | Happy Path | Logged in as `rahulshetty1@gmail.com`. The static event "Hollywood Monsoon Night — Los Angeles" exists (seeded, `isStatic: true`, price $2,500, 3000 seats). No prior bookings for this event in this session so `personalAvailable` ≥ 1. | 1. Navigate to `http://localhost:3000/events`. 2. Locate the "Hollywood Monsoon Night — Los Angeles" event card and click it. 3. Verify the booking panel shows price **$2,500** per ticket and `#ticket-count` displays `1`. 4. Enter `Jane Smith` in `#customerName`. 5. Enter `rahulshetty1@gmail.com` in `#customer-email`. 6. Enter `+91-9876543210` in `#phone`. 7. Click `#confirm-booking`. 8. Wait for the confirmation card ("Booking Confirmed! 🎉") to appear. 9. Read and store the value of `.booking-ref` (e.g. `H-A1B2C3`). 10. Verify the Total row on the confirmation card shows **$2,500**. 11. Click "View My Bookings". 12. On `/bookings`, locate the card with the stored booking ref and click "View Details". 13. On the booking detail page (`/bookings/:id`): read the **Booking ID** value under the "Booking Information" section (rendered as `#booking.id`), read **Total Paid** under "Payment Summary", and read **Tickets** count. | - Confirmation card heading is "Booking Confirmed! 🎉". - `.booking-ref` matches regex `/^H-[A-Z0-9]{6}$/` (prefix `H` = first char of "Hollywood" uppercased). - Confirmation card **Total** = `$2,500` (1 × $2,500). - Booking detail page breadcrumb shows the stored booking ref. - "Booking Information" section shows **Booking ID** as a positive integer (e.g. `#14`). - "Payment Summary" shows **Tickets** = `1`, **Price per ticket** = `$2,500`, **Total Paid** = `$2,500`. - "Event Details" section shows: Event = "Hollywood Monsoon Night — Los Angeles", Category = "Concert", City = "Los Angeles". - "Customer Details" section shows: Name = "Jane Smith", Email = `rahulshetty1@gmail.com`. - Booking status badge = `confirmed`. - "Cancel Booking" button is visible. | "Booking ref first character = event title first character (uppercase)" (R25). Static events always available for practice — `isStatic: true`, seats computed per-user in `eventService.js`. `totalPrice` = `price × quantity` enforced in `bookingService.js`. | E2E |
| HMN-002 | Business Rules | Logged in as `rahulshetty1@gmail.com`. The static event "Hollywood Monsoon Night — Los Angeles" exists (seeded, price $2,500). No prior bookings for this event in this session so `personalAvailable` ≥ 3. | 1. Navigate to `http://localhost:3000/events`. 2. Click the "Hollywood Monsoon Night — Los Angeles" event card. 3. On the booking panel click the `+` button **twice** so `#ticket-count` reads `3`. 4. Verify the live price summary shows **$2,500 × 3 tickets** on the left and **$7,500** on the right, and the **Total** row shows **$7,500** in indigo text. 5. Enter `Alex Kumar` in `#customerName`. 6. Enter `rahulshetty1@gmail.com` in `#customer-email`. 7. Enter `+91-9876543210` in `#phone`. 8. Click `#confirm-booking`. 9. Wait for the confirmation card to appear. 10. Read and store `.booking-ref`. 11. Verify the **Total** row on the confirmation card shows **$7,500**. 12. Click "View My Bookings". 13. Locate and click the card matching the stored booking ref. 14. On `/bookings/:id`: read **Booking ID** (under "Booking Information"), **Tickets** count, and **Total Paid** (under "Payment Summary"). 15. Click `[data-testid="check-refund-btn"]` and wait ~4 s for the result. | - Confirmation card **Total** = `$7,500` (3 × $2,500). - `.booking-ref` matches `/^H-[A-Z0-9]{6}$/`. - Booking detail **Tickets** = `3`. - **Price per ticket** = `$2,500`. - **Total Paid** = `$7,500`. - **Booking ID** is a positive integer (e.g. `#17`). - `[data-testid="refund-result"]` text contains "Not eligible for refund. Group bookings (3 tickets) are non-refundable." - Refund result has red background (`.bg-red-50`). - No mutation occurs to global seat count (static event seats remain unaffected across users). | "Total price = price × quantity" (R10). "Booking ref first character = event title first character (uppercase)" (R25). "Refund eligibility: 1 ticket = eligible, >1 ticket = not eligible (client-side)" (R15) — computed in `BookingDetailPage` `RefundEligibility` component via `setTimeout` 4000 ms. Static events never decrement global seats — `eventService.js` computes `personalAvailable`. | E2E |
