# Booking Creation — Dilli Diwali Mela (Booking ID & Total Amount)

**Feature area:** Booking Creation — book tickets for the static "Dilli Diwali Mela" event, capture the Booking ID and total amount, and verify all detail fields on the booking detail page  
**Generated:** 2026-06-02  
**Total scenarios:** 2

| TC No | Category | Preconditions | Steps | Expected Result | Business Rules | Suggested Layer |
|---|---|---|---|---|---|---|
| DDM-BK-001 | Happy Path | Logged in as `rahulshetty1@gmail.com`. The static event "Dilli Diwali Mela" exists (seeded, `isStatic: true`, Category: Festival, City: Delhi, price $300, 10 000 seats). No prior bookings for this event in this session so `personalAvailable` ≥ 1. | 1. Navigate to `http://localhost:3000/events`. 2. Locate the "Dilli Diwali Mela" event card and click it. 3. Verify the booking panel shows price **$300** per ticket and `#ticket-count` displays `1`. 4. Enter `Jane Smith` in `#customerName`. 5. Enter `rahulshetty1@gmail.com` in `#customer-email`. 6. Enter `+91-9876543210` in `#phone`. 7. Click `#confirm-booking`. 8. Wait for the confirmation card ("Booking Confirmed! 🎉") to appear. 9. Read and store the value of `.booking-ref` (e.g. `D-A1B2C3`). 10. Verify the **Total** row on the confirmation card shows **$300**. 11. Click "View My Bookings". 12. On `/bookings`, locate the booking card with the stored ref and click "View Details". 13. On `/bookings/:id`: read the **Booking ID** value under "Booking Information" (rendered as `#booking.id`, e.g. `#51`), read **Total Paid** under "Payment Summary", and read **Tickets** count. | - Confirmation card heading is "Booking Confirmed! 🎉". - `.booking-ref` matches regex `/^D-[A-Z0-9]{6}$/` (prefix `D` = first char of "Dilli" uppercased). - Confirmation card **Total** = `$300` (1 × $300). - Booking detail page breadcrumb shows the stored booking ref. - "Booking Information" section shows **Booking ID** as `#N` where N is a positive integer. - "Payment Summary" shows **Tickets** = `1`, **Price per ticket** = `$300`, **Total Paid** = `$300`. - "Event Details" section shows: Event = "Dilli Diwali Mela", Category = "Festival", City = "Delhi". - "Customer Details" section shows: Name = "Jane Smith", Email = `rahulshetty1@gmail.com`. - Booking status badge = `confirmed`. - "Cancel Booking" button is visible. - "Check eligibility for refund?" link is visible (idle state). | "Booking ref first character = event title first character (uppercase)" (R25) — prefix `D` for "Dilli Diwali Mela". `totalPrice` = `price × quantity` enforced in `bookingService.js` (R10). Static events always available for practice — `isStatic: true`, seats computed per-user in `eventService.js`. | E2E |
| DDM-BK-002 | Business Rules | Logged in as `rahulshetty1@gmail.com`. The static event "Dilli Diwali Mela" exists (seeded, price $300). No prior bookings in this session so `personalAvailable` ≥ 4. | 1. Navigate to `http://localhost:3000/events`. 2. Click the "Dilli Diwali Mela" event card. 3. On the booking panel click the `+` button **three times** so `#ticket-count` reads `4`. 4. Verify the live price summary shows **$300 × 4 tickets** on the left and **$1,200** on the right, and the **Total** row shows **$1,200** in indigo text. 5. Enter `Alex Kumar` in `#customerName`. 6. Enter `rahulshetty1@gmail.com` in `#customer-email`. 7. Enter `+91-9876543210` in `#phone`. 8. Click `#confirm-booking`. 9. Wait for the confirmation card to appear. 10. Read and store `.booking-ref`. 11. Verify the **Total** row on the confirmation card shows **$1,200**. 12. Click "View My Bookings". 13. Locate and click "View Details" on the card matching the stored ref. 14. On `/bookings/:id`: read **Booking ID** (under "Booking Information"), **Tickets** count, and **Total Paid** (under "Payment Summary"). 15. Click `[data-testid="check-refund-btn"]` and wait up to 6 s for the result. | - Confirmation card **Total** = `$1,200` (4 × $300). - `.booking-ref` matches `/^D-[A-Z0-9]{6}$/`. - Booking list card shows "4 tickets". - Booking detail **Tickets** = `4`. - **Price per ticket** = `$300`. - **Total Paid** = `$1,200`. - **Booking ID** is `#N` where N is a positive integer greater than the ID from DDM-BK-001 (sequential). - `[data-testid="refund-spinner"]` appears immediately after clicking the refund button. - After ~4 s, `[data-testid="refund-result"]` appears containing "Not eligible for refund. Group bookings (4 tickets) are non-refundable." with red background (`.bg-red-50`). - No mutation to global seat count (static event, `isStatic: true`). | "Total price = price × quantity" (R10) — 4 × 300 = 1 200. "Booking ref first character = event title first character (uppercase)" (R25). "Refund eligibility: 1 ticket = eligible, >1 ticket = not eligible (client-side)" (R15) — enforced in `RefundEligibility` component via `setTimeout(4000)`. Static events never decrement global seats — `eventService.js` computes `personalAvailable`. | E2E |
